syntax = "proto3";
package authly_service;

service AuthlyService {
    rpc Metadata (Empty) returns (ServiceMetadata);
}

message ServiceMetadata {
    string eid = 1;
    string label = 2;
}

message Empty {}
